1. Create a test page called `/dsl-auto-complete`. For the mini-dsl that was added to do per-link changes in batches I want to be able to do suggestions for people that forget syntax. So for example someone types `!t` they should get autocomplete options for `!tag=[]` or `!topic=[]`
    1. Additionally for tags and topics there should be autocomplete for existing tag names, so user types `!tag=[r]` a dropdown should appear with `rust` if it's in the tag list. 
    2. Do NOT actually implement this on the main component. As with similar experiments setup a test page, and a test component that is separate from any currently used components. If these feature proves to be too much of an issue, I want to just have to delete the experimental pages
2. Create a test page called `/bulk-operations-test`, in this page give me a layout that is the same as the standard backlog page, but I want to be able to bulk modify **EXISTING** items. Similar to how in wordpress and drupal if you look at the content overview pages, you can select a range of pages, and apply operations to them. I want to be able to do that. Ideally I should be able to check off any of these items and batch add/remove tags/topics, whether the links are favourites, resources, done, and which week they're on

