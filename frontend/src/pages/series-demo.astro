---
import Layout from '../layouts/Layout.astro';
import SeriesDemoApp from '../components/apps/SeriesDemoApp.svelte';
---

<!--
  Prototype page for the series design (docs/dev/experiments & plans/series.md).
  Deliberately not in the nav: it is a design surface, not a feature, and its
  state lives in localStorage rather than the database.
-->
<Layout title="Series (demo)" hasCapture={false}>
  <SeriesDemoApp client:only="svelte" />
</Layout>
