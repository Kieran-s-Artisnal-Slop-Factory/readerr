- [x] Move `Backlog` and `Favourites` links under `Collections`
- [x] Add an `inbox` tab that allows you to subscribe to RSS feeds, and then triage the links into ones you want to add to the backlog/current week reading list, and ones you don't care about
  - [x] Example sites
    - [x] https://blog.cloudflare.com/
      - [x] https://blog.cloudflare.com/rss/
    - [x] https://thedailywtf.com/
      - [x] https://feeds.feedburner.com/TheDailyWtf
  - [x] Check each feed once per day (with option to manually refresh and bulk refresh all)
  - [x] Links (that are not in reading week/backlog) show up and you can add them to this week or backlog, or ignore them
  - [x] When adding a new feed, allow people to pull from a certain number of days ago (e.g. on setup pull last 30 days worth of articles)
- [x] Come up with a plan and put it in `series.md` for how to better handle links that are for series (e.g. part 1, part 2, etc.)
      (plan: `docs/dev/experiments & plans/series.md`; demo page: `/series-demo/`)
  - [x] A series is like a file folder, it contains the other links that make it up, but it can be used itself like a normal link (favourited, added to a week, show up in reading lists)
  - [x] Create a demo page with a UI I can test with 
    - [x] Add a `add series` that pulls up a modal that allows me to put in:
      - [x] A series title
      - [x] Series description
      - [x] Overview URL
      - [x] Tags & topics
          - [x] On a per-series and per-link basis
      - [x] The links themselves
        - [x] Position in series (e.g. 1, 2, 3, etc.)
        - [x] URL
        - [x] title
        - [x] Assignmed weel
    - [x] Create UI around how it would look in reading lists, with drop-down nesting to make it obvious if a link is part of a series, or if the series was added, the abiltiy to update individual links as you read them
- [x] On the tags page add a search bar
- [x] On the stats page show a distribution of tags by percentage



Ensure for each change:

- if data model is updated
    - There are no sync issues/regressions with the changes
    - There are adequete tests for the system to ensure no synchronization issues, and that the system works both from the UI and has unit tests
    - Features that touch frontend and backend have testing and validation for both peices
- Add Changes to the CHANGELOG.md
    - If a version exists and does not have a release date yet, keep adding to it, if there's no versions without a release date, then bump version (e.g. `v0.1.0 -> v0.2.0`) in the version file, and add a new main heading to CHANGELOG.md for it (e.g. `v0.2.0 (unreleased)`) and add the changes under there.
    - Add all changes under the following categories as sub-headings:
      - Features
      - Bug Fixes
      - Other
        - For things like
          - Documentation improvements
          - Refinements to existing systems that weren't bugs
          - anything else that doesn't fit in the other sections
