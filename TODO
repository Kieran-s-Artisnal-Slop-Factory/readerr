# Version 0.4.0 plans

Before beginning make sure the VERSION is up to date, and create a file called `docs/dev/plans/version-<version number>-phased.md` which is a phased plan for implementing the features, then stop after creating it. If that file exists, the that means the version has been started and you're taking over from where another agent left off. If it doesn't then create the file making sure to also create "checkpoints" which are points after a few phases where the full test suite is ran, and must pass before proceeding. You don't need to run the full tests on every phase because it's going to be too slow. For each checkpoint, if it's not going to trample one another, feel free to use subagents, in fact ideally you should.

Additional things to keep in mind:
- Usability takes priority over accessibilty when the two collide. This app is a personal app, primarily used by me, not one being shared with people who have accessibility needs. It's nice for it to be usable, but not at the cost of functionality or performance.
- When documenting features, in the `dev` docs be sure to include mermaid diagrams, and code references to make it clear and simple for people to find what they need to understand.

## Maintenance

- [x] Check all dependencies and do updates
  - [x] Frontend
  - [x] Backend

## Features & fixes

- [x] Only sync when a valid URL is set as a sync url
  - [x] There are currently constant errors in the console log
    - [x] Related to posts to `/healthz`, `/sync/push`, and `/sync/stats`
    - [x] When on inbox page, getting constant errors on `/feed`
- [x] When I am typing to look for a link (e.g. in resource list page, the `Paste a URL to add, or search your links...`), I need to be able to scroll results, or have a next-page option. Large libraries of links far exceed the current number shown
- [x] Bulk operations; When bulk selecting links such as in the reading list tab
  - [x] allow me to add them to resource lists
  - [x] Improve the UI
    - [x] on the reading list page so that if I'm selecting items in the `done` section, the controls appear in the `done` section. Currentl selecting links in the `done` section causes the UI to appear at the top of `this week` which can take a while to scroll to
- [ ] Topics (all but the tag-export metadata, which waits on phases 9–10)
  - [x] Statuses
    - [x] Allow me to mark topics as `in-progress` and `done`
      - [x] These statuses are optional, but when set change the display and filtering behavior on the topics overview page (page that lists all topics)
        - [x] In progress acts as a way to more highly weight a topic, and should appear at the top of the topics page
        - [x] Done items should appear below in-progress and items with no status
  - [x] Add bulk operations
    - [x] Delete
    - [x] Assign status (in-progress, done)
    - [x] Remove status
    - [x] Assign tags
    - [x] remove tags
  - [x] Allow assigning tags to topics
    - [x] This makes them show up on that tags overview page
    - [ ] Show up as a metadata item in the export for that tag (blocked: the tag export itself is phases 9–10)
    - [x] Show the tags as a metadata item (frontmatter for md, in header card for HTML)
  - [x] Add filters and search to topics overview
    - [x] Add the ability to search
    - [x] Add the abilty to filter on the topics page by
      - [x] Tag
      - [x] Status
- [ ] In the link overview page (page that lets you take exerpts, write notes etc. for a single link)
  - [ ] Add the ability to reschedule a link
  - [ ] Add the ability for me to assign the item to a resource list (should mark it as a resource if it isn't already)
- [ ] Exportable tags, similar to the exportable resource lists
  - [ ] Unify resource list exporting and exportable tags. Make sure both have the same features that make sense
  - [ ] Allow for tags to be an exportable HTML page or markdown file
    - [ ] Markdown
      - [ ] Frontmatter
        - [ ] Child tags
        - [ ] Number of links
          - [ ] Direct links
          - [ ] Child links
          - [ ] Number of favourites
      - [ ] Include about section
      - [ ] Topics
        - [ ] Embed topic information in metadata section of documents
        - [ ] Include option to embed topic info as a checkbox
          - [ ] For HTML this puts the topic into a modal that opens when clicked with the topic content
          - [ ] for MD this just adds sections to the bottom of the document that have the content from the topics
            - [ ] Add checkbox to export each topic as a separate file if that's what they want, then download them all as a zip
      - [ ] Put links into separate sections with headings
        - [ ] Links directly associated with tags
        - [ ] "From child tags"
        - [ ] Ordering
          - [ ] 1. Favourites
          - [ ] 2. Read
          - [ ] 3. Unread
        - [ ] Columns
          - [ ] link
          - [ ] Read
            - [ ] true or false
          - [ ] Favourite
            - [ ] true or false
          - [ ] Resource
            - [ ] true or false
          - [ ] Reading week
          - [ ] tags
            - [ ] comma separated list (escape `|`)
    - [ ] HTML
      - [ ] Single HTML page (all JS/css inlined)
      - [ ] Same theme as selected theme
      - [ ] Metadata stats in header card
        - [ ] Child tags
        - [ ] Number of links
          - [ ] Direct links
          - [ ] Child links
          - [ ] Number of favourites
      - [ ] About section as rendered html from markdown (if available)
      - [ ] Separate tables for directly associated links, and links from child tags
        - [ ] Default ordering
          - [ ] 1. Favourites
          - [ ] 2. Read
          - [ ] 3. Unread
        - [ ] Include filterable and sortable datatable from retoken for each column
        - [ ] Columns
          - [ ] link
          - [ ] Read
            - [ ] true or false
          - [ ] Favourite
            - [ ] true or false
          - [ ] Resource
            - [ ] true or false
          - [ ] Reading week
          - [ ] tags
            - [ ] comma separated list

## For human

This section is for me to keep track of what I need to manually review, feel free to add to this

- [ ] Test series
  - [ ] https://blog.cloudflare.com/
    - [ ] https://blog.cloudflare.com/rss/
  - [ ] https://thedailywtf.com/
    - [ ] https://feeds.feedburner.com/TheDailyWtf
  - [ ] https://feeds.megaphone.fm/FSI1483080183
  - [ ] https://feeds.transistor.fm/postgres-fm
  - [ ] https://blog.flipper.net/rss/
  - [ ] https://daverupert.com/atom.xml
  - [ ] https://rustfoundation.org/media/category/blog/feed/
  - [ ] https://feeds.transistor.fm/database-school

## Per change
Required guidence to follow per change

- Check off changes as you go
  - Make sure the TODO is kept up to date so that if a usage limit is hit I'm not left in the dark
- if data model is updated
    - There are no sync issues/regressions with the changes
    - There are adequete tests for the system to ensure no synchronization issues, and that the system works both from the UI and has unit tests
    - Features that touch frontend and backend have testing and validation for both peices
- Add Changes to the CHANGELOG.md
    - If a version exists and does not have a release date yet, keep adding to it, if there's no versions without a release date, then bump version (e.g. `v0.1.0 -> v0.2.0`) in the version file, and add a new main heading to CHANGELOG.md for it (e.g. `v0.2.0 (unreleased)`) and add the changes under there.
    - Be brief, 2-4 setnences per change, and if you need more, prompt for approval from the user
    - Add all changes under the following categories as sub-headings:
      - Features
      - Bug Fixes
      - Other
        - For things like
          - Documentation improvements
          - Refinements to existing systems that weren't bugs
          - anything else that doesn't fit in the other sections
