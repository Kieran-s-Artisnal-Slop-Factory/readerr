All four done — see docs/user/organizing-and-reading.md and docs/dev/data-model.md.

1. [x] Allow me to add/remove a link from a week, (handling if it's a review or first time viewing) directly  on it's overview page `/link?id=...`
2. [x] On topics pages allow me to use referenced links as footnote references with the standard markdown format of `[^1]`.
    1. [x] Will need to be stable if old references are deleted, so an autoincrementing number from the last value should work
    2. [x] E.g. I have 3 links, you would need to order them 1-3, store those numbers, then allow me in the editor to do `[^1]` to reference link 1, `[^2]` to reference link 2, etc.
    3. [x] You can probably handle this by making an explicit `footnotes` section at the bottom that just has `[^1]: <url>`, etc.
       — generated at export time rather than written into body_md, so the
         document stays exactly what you typed.
3. [x] Add an HTML export for topics pages that includes the references. This should match the readerr theme
4. [x] Improve the mobile experience, text is getting far too agressively truncated and link-rows are unreadable on mobile
       — link rows stack below 40rem: titles were getting 42px of a 375px
         screen (20% of the text), now they wrap in full.
