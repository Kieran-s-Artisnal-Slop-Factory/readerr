1. Add improvements to stats page
    1. Add a toggle to visually hide domains with only 1 link
    2. Add a "variability" metric that is a weighted average of how many links come from outside your top 3 domains
        1. For example if `youtube.com`, `medium.com`, and `twitter.com` are your top 3 domains, you have 1,200 links and those 3 domains account for 980 of them, then your variability score would be `((1200-900)/1200)*100`
2. 

Ensure for each change:

- There are no sync issues/regressions with the changes
- There are adequete tests for the system to ensure no synchronization issues, and that the system works both from the UI and has unit tests
- Features that touch frontend and backend have testing and validation for both peices

