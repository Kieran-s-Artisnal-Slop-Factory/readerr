- [ ] Add an `inbox` tab that allows you to subscribe to RSS feeds, and then triage the links into ones you want to add to the backlog/current week reading list, and ones you don't care about
- [ ] Come up with a plan and put it in `series.md` for how to better handle links that are for series (e.g. part 1, part 2, etc.)
  - [ ] A series is like a file folder, it contains the other links that make it up, but it can be used itself like a normal link (favourited, added to a week, show up in reading lists)
  - [ ] Create a demo page with a UI I can test with 
    - [ ] Add a `add series` that pulls up a modal that allows me to put in:
      - [ ] A series title
      - [ ] Series description
      - [ ] Overview URL
      - [ ] Tags & topics
          - [ ] On a per-series and per-link basis
      - [ ] The links themselves
        - [ ] Position in series (e.g. 1, 2, 3, etc.)
        - [ ] URL
        - [ ] title
        - [ ] Assignmed weel
    - [ ] Create UI around how it would look in reading lists, with drop-down nesting to make it obvious if a link is part of a series, or if the series was added, the abiltiy to update individual links as you read them
- [ ] On the stats page show a distribution of tags by percentage



Ensure for each change:

- if data model is updated
    - There are no sync issues/regressions with the changes
    - There are adequete tests for the system to ensure no synchronization issues, and that the system works both from the UI and has unit tests
    - Features that touch frontend and backend have testing and validation for both peices
- Add Changes to the CHANGELOG.md
    - If a version exists and does not have a release date yet, keep adding to it, if there's no versions without a release date, then bump version (e.g. `v0.1.0 -> v0.2.0`) in the version file, and add a new main heading to CHANGELOG.md for it (e.g. `v0.2.0 (unreleased)`) and add the changes under there.
    - Add all changes under the following categories as sub-headings:
      - Features
      - Bug Fixes
      - Other
        - For things like
          - Documentation improvements
          - Refinements to existing systems that weren't bugs
          - anything else that doesn't fit in the other sections
